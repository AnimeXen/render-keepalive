name = "render-keepalive"
main = "src/index.js"
compatibility_date = "2025-12-25"

[triggers]
crons = ["*/5 * * * *"]  # Runs every 10 minutes

# Optional: Add multiple Render URLs as environment variables
[vars]
RENDER_URL = "https://your-app.onrender.com"
